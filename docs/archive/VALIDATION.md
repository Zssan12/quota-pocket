# v0.1.3 测试版验证

> 历史记录，内容对应当时版本，不作为当前安装或支持范围说明。请从[项目首页](../../README.md)开始。

当前发布范围：Mac 采集 → 白名单快照 → 用户 iCloud → iPhone Scriptable。

## 自动化

2026-09-14 本机运行 `npm test`：91 项 Python 测试通过，Scriptable 与配置引导 JavaScript 测试通过。包括真实文件导出到安装脚本的贯穿测试；上游和 iOS API 使用测试替身。

覆盖凭证字段过滤、采集失败保留旧值与原时间、账户范围撤回、离线缓存、安装隔离、三种尺寸的重置时间、分组轮换和配置引导。测试指定的 iCloud 目录会在重新加载状态后保留，防止测试向真实 Scriptable 目录写入脚本。

## 实机结果与限制

开发机曾完成 11 个账户采集和 iCloud 写入，用户确认真实 iPhone 收到更新。这不是所有供应商或所有系统版本的兼容保证。

尚未完成：全新 Mac 环境、后台文件权限授予后的登录启动、睡眠唤醒、锁屏、低电量及网络切换的完整验收。最新轮换逻辑已通过模拟测试，仍需真机视觉确认。

后台权限未解决时采用手动启动。不能把进程启动成功、Mac 写入成功或模拟测试通过当作桌面及时刷新成功。

## 复现

```sh
npm ci --ignore-scripts
npm test
```

测试产物位于 `output/test-runs`，不纳入 Git。不要提交本地状态、日志或真实额度截图。

## 2026-09-15 发布副本检查

在独立目录中仅复制发布文件，不携带账户状态和 node_modules，运行 `npm ci --ignore-scripts` 成功，npm 审计报告 0 个已知漏洞；91 项 Python 测试和两组 JavaScript 测试全部通过。环境为 macOS、Python 3.9.6、Node.js 25.8.1。

使用新的状态目录和临时本机端口启动，首页可访问、账户为空、iCloud 与旧同步默认关闭，进程正常退出。未复用开发机已安装运行副本的状态。这属于当前 Mac 上的干净目录验证，不等于全新 Mac 或 Node.js 20 已实测。

已配置 GitHub Actions（macOS、Python 3.11、Node.js 20）；远端执行结果需上传后确认。

## 2026-09-15 一行安装入口

新增安装器后，97 项 Python 测试及 Scriptable、配置引导测试全部通过。新增覆盖：重复安装保留代码与账户、依赖安装失败不修改运行状态、端口冲突不启动新进程、复用已认证服务、启动快捷文件路径转义、缺少 Python 时提前给出指引。

使用独立空目录完成真实 npm 依赖安装和本机启动，随后重复运行，再停止并重新启动。账户凭证与 iCloud 安装 ID 保持不变，未触碰开发机实际采集器。自动打开浏览器的调用已通过模拟测试；本轮进程集成验证关闭了浏览器弹窗。

安装器要求已有 Python 3.9+、Node.js 20+ 和 npm，缺少时提示安装，不自动安装系统工具。已下载源码可执行 `bash install.sh --source "$PWD"`。仓库目标为 `Zssan12/quota-pocket`；远程安装入口需要仓库与 `v0.1.3` 标签可访问。本机验证不能代替远程下载验证。

## v0.2.0 多订阅账号

新增 13 项隔离回归测试：4 个订阅账号的唯一 ID 与白名单导出；旧凭证和手机选择不变；单账号失败与退避；修改备注、停用和恢复；重新授权只影响目标账号；设置页保存不误改账号开关；旧在途查询不能覆盖新配置；非法目标和授权期间修改被拒绝；新增/失败授权保留已有账号；两个 Codex 登录目录与两个 Claude Token 独立使用；管理接口权限。

Playwright 使用独立状态目录和模拟账号验证网页新增第三个 ChatGPT 账号、修改备注（含 HTML 特殊字符）、停用、启用、重新授权和 Claude 列表。320 / 390 / 1440 像素宽无横向溢出。没有通过浏览器自动登录用户的真实账号。

本轮完整 `npm test`：110 项 Python 测试及 Scriptable / 配置引导 JavaScript 测试全部通过。
