{
  "name": "quota-pocket",
  "version": "0.2.0",
  "private": true,
  "description": "Read-only AI subscription and provider quotas on your phone",
  "type": "module",
  "scripts": {
    "start": "python3 server.py",
    "test": "python3 -m unittest discover -s tests -v && node tests/test_scriptable.mjs && node tests/test_setup.mjs"
  },
  "dependencies": {
    "quickjs-emscripten": "0.31.0"
  }
}
